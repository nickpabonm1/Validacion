# Valor por defecto NO sensible (ruta de archivo SQLite local). No requiere configuración
# manual para desarrollo/demo. Para producción con Postgres, sobrescriba DATABASE_URL en el
# entorno del servidor y cambie el `provider` del datasource en schema.prisma.
DATABASE_URL="file:./dev.db"
